import React from 'react';
import {connect} from 'react-redux';

class betIndex extends React.Component {
  render () {
    return (
      <h1> This is the complete bet history page </h1>
    );
  }
}

export default betIndex;
